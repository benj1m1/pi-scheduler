#!/bin/sh
set -eu

APP_DIR=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
PYTHON=${PI_SCHEDULER_PYTHON:-$APP_DIR/.venv/bin/python3}

if [ ! -x "$PYTHON" ]; then
  PYTHON=python3
fi

export PYTHONPATH="$APP_DIR${PYTHONPATH:+:$PYTHONPATH}"
exec "$PYTHON" -m app.runner "$@"
